# Keybinds
unbind C-b
set-option -g prefix C-a

bind -n C-h select-pane -L
bind -n C-j select-pane -D
bind -n C-k select-pane -U
bind -n C-l select-pane -R

bind '"' split-window -v -c "#{pane_current_path}"
bind '%' split-window -h -c "#{pane_current_path}"

#setw -g mode-keys vi

# Misc
set -g mouse on

# Format and Style
## Status
set -g status-style "fg=#665c54"
set -g status-bg default
set -g status-position top
set -g status-interval 1
set -g status-left ""

set -g window-status-current-format "#[bold]#I:#W*"
set -g window-status-current-style "fg=#968c84",bg=default
set -g window-status-last-style "fg=#968c84",bg=default

## Window names
set -g automatic-rename

## Pane
set -g pane-border-style "fg=#665c54"
set -g pane-active-border-style "fg=#000080"

## Message
set -g message-style "fg=#8a8a8a"

## Clock
set -g clock-mode-style 12
set-option -g clock-mode-colour yellow

## Index of new windows/panes/etc
set -g base-index 1
setw -g pane-base-index 1

# Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'

# Initialize TMUX plugin manager
run '~/.tmux/plugins/tpm/tpm'
